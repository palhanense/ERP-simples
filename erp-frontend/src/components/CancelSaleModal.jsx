// CancelSaleModal: archived
// This modal component appears unused. Kept as a stub to avoid accidental breakage.
export default function CancelSaleModal() {
  if (typeof console !== 'undefined') {
    console.warn('CancelSaleModal is archived and should not be used.');
  }
  return null;
}
